version: "1.0"

services:
  api:
    build:
      context: .
      target: development
    volumes:
      - ./:usr/src/app
      - /user/src/app/node_modules
    ports:
      - 8888:8888
    command: npm run dev